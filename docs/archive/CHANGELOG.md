# 📝 Changelog

جميع التغييرات المهمة في هذا المشروع موثقة في هذا الملف.

الصيغة مبنية على [Keep a Changelog](https://keepachangelog.com/ar/1.0.0/)،
ويتبع هذا المشروع [Semantic Versioning](https://semver.org/lang/ar/).

---

## [Unreleased]

### قيد التطوير
- نظام الأدوار والصلاحيات (TASK-026)
- سجل التدقيق Activity Log (TASK-027)
- فلاتر متقدمة وبحث فوري (TASK-028-029)
- واجهة موبايل Responsive (TASK-030)
- اختبارات شاملة Unit/Integration/E2E (TASK-031-033)

---

## [1.0.0] - 2025-10-03

### ✨ النسخة الأولى - نظام إدارة المخزون المتكامل

هذا هو الإصدار الأول الكامل للنظام مع 25 ميزة رئيسية.

---

### 🎯 الميزات الأساسية (TASK-001 إلى TASK-010)

#### Added
- **البنية الأساسية** (TASK-001)
  - تنصيب Laravel 12.32.5 مع PHP 8.2
  - قاعدة بيانات SQLite (افتراضي)
  - Bootstrap 5.3 RTL
  - DomPDF للطباعة
  - Spatie Packages (Permission + Activity Log)

- **إدارة الفروع** (TASK-003)
  - 3 فروع: المصنع، العتبة، إمبابة
  - CRUD كامل مع Validation
  - منع حذف فرع مرتبط بمنتجات

- **إدارة التصنيفات** (TASK-004)
  - CRUD للتصنيفات
  - ربط بالمنتجات
  - Validation: اسم فريد

- **كارت الصنف** (TASK-005)
  - جدول Products: SKU (فريد)، الاسم، الماركة، التصنيف، وحدة القياس، حجم العبوة، حد أدنى، حالة تفعيل
  - Form إدخال RTL كامل
  - Validation صارمة
  - إخفاء المنتجات غير النشطة (soft disable)

- **ربط المنتج بالفروع** (TASK-006)
  - جدول `product_branch_stock`: رصيد حالي + حد أدنى لكل منتج/فرع
  - تحديث الرصيد تلقائياً عند حركات المخزون
  - واجهة لتعديل الحد الأدنى

- **خدمة التسلسل** (TASK-007)
  - `SequencerService`: ترقيم متسلسل بدون فجوات
  - جدول `sequences`: 3 أنواع (ISSUE, RETURN, TRANSFER)
  - `SELECT ... FOR UPDATE` لمنع التعارض
  - نطاقات:
    - ISSUE: 1-999,999
    - RETURN: 100,001-125,000
    - TRANSFER: 1-999,999

- **خدمة المخزون** (TASK-008)
  - `InventoryService`: إدارة حركات المخزون
  - جدول `inventory_movements`: 5 أنواع حركات
  - **منع تام للرصيد السالب** عبر Transactions
  - تحديث `product_branch_stock.current_qty` تلقائياً
  - Indexes محسّنة للأداء

- **إذن صرف - مسودة** (TASK-009)
  - جداول: `issue_vouchers` + `issue_voucher_items`
  - نوعان: بيع (SALE) أو تحويل (TRANSFER)
  - خصومات: على مستوى البند أو الفاتورة (% أو مبلغ)
  - دفع: نقدي (CASH) أو آجل (CREDIT)
  - حالة DRAFT: لا تأثير على المخزون
  - إدخال بالكرتونة: qty_units = cartons × pack_size + units

- **إذن صرف - اعتماد** (TASK-010)
  - `IssueVoucherService::approve($voucher)`
  - **داخل Transaction**:
    1. إسناد رقم تسلسلي من `SequencerService`
    2. تحديث status=APPROVED
    3. خصم المخزون عبر `InventoryService` (مع منع رصيد سالب)
    4. إضافة قيد دفتر عميل (إن SALE+CREDIT)
    5. إنشاء TRANSFER_IN تلقائي (إن TRANSFER)
  - Activity Log: تسجيل من اعتمد ومتى
  - Rollback كامل إن فشلت أي خطوة

---

### 💰 إدارة العملاء والمدفوعات (TASK-011 إلى TASK-013)

#### Added
- **إذن ارتجاع** (TASK-011)
  - جداول: `return_vouchers` + `return_voucher_items`
  - ترقيم خاص: 100,001-125,000 (مع تحقق من النطاق)
  - **عند الاعتماد**:
    - إسناد رقم من النطاق المحدد
    - إضافة للمخزون عبر `InventoryService` (RETURN)
    - قيد دائن (له) للعميل
  - سبب الارتجاع (reason) إلزامي

- **دفتر العملاء** (TASK-012)
  - جدول `customers`: code (فريد)، name، phone، address، is_active، last_activity_at
  - جدول `customer_ledger_entries`: debit (علية) + credit (له)
  - **الرصيد المتحرك**: Σ(debit - credit)
  - واجهة دفتر الحساب: جدول الحركات + رصيد حالي
  - فلاتر: من/إلى، نوع القيد
  - Validation: debit أو credit فقط (ليس كلاهما)

- **المدفوعات والشيكات** (TASK-013)
  - جدول `payments`: customer_id، amount، method (CASH/CHEQUE)
  - جدول `cheques`: cheque_number، bank، due_date، status (PENDING/CLEARED/RETURNED)
  - **عند المدفوع**: قيد credit (له) تلقائي
  - ربط الشيك بفاتورة (linked_issue_voucher_id)
  - تقرير: جرد شيكات غير مصروفة (status=PENDING)
  - تحصيل/إرجاع شيك: تغيير الحالة + cleared_at

---

### 🔔 التنبيهات والطباعة (TASK-014 إلى TASK-016)

#### Added
- **تنبيهات حد أدنى** (TASK-014)
  - استعلام: `current_qty < min_qty`
  - Dashboard Widget: قائمة أصناف منخفضة (أعلى 10)
  - تقرير: نقص المخزون لكل فرع
  - Edge Case: min_qty=0 → لا تنبيه

- **تحقق حجم العبوة** (TASK-015)
  - إعداد: `enforce_full_pack=false` (السماح بكسر عبوة مع تنبيه)
  - عند إدخال كمية: `qty_units % pack_size !== 0` → تنبيه أصفر
  - Edge Case: pack_size=null → لا تحقق
  - لا يمنع الحفظ، فقط تنبيه

- **قوالب الطباعة PDF** (TASK-016)
  - تنصيب خط DejaVu Sans للعربية
  - **قوالب Blade**:
    1. `issue-voucher-pdf.blade.php` (إذن صرف)
    2. `return-voucher-pdf.blade.php` (إذن ارتجاع)
    3. `customer-statement-pdf.blade.php` (كشف حساب)
  - محتوى: شعار، بيانات، جدول بنود، إجماليات، توقيعات
  - DomPDF: تصدير PDF (A4 Portrait)
  - زر "طباعة" متاح فقط بعد الاعتماد

---

### 📥 استيراد من Excel/CSV (TASK-017 إلى TASK-019)

#### Added
- **استيراد المنتجات والأرصدة** (TASK-017)
  - `ProductStockImport.php`: استيراد CSV بـ native `fgetcsv()`
  - قالب: (branch_code, sku, qty_units)
  - Validation: SKU موجود، branch_code صحيح، qty ≥ 0
  - **عند الاعتماد**: `InventoryService::applyMovement('ADD', ...)` لكل سطر
  - ref_table='OPENING'
  - Edge Case: سطر خطأ → تخطي + تسجيل Log

- **استيراد العملاء** (TASK-018)
  - `CustomerImport.php`
  - قالب: (customer_code, name, phone, address, opening_balance)
  - **عند الاعتماد**:
    - إنشاء Customer
    - إضافة قيد افتتاحي: debit (علية) إن موجب، credit (له) إن سالب
  - Validation: customer_code فريد
  - Edge Case: opening_balance=0 → لا قيد

- **استيراد الشيكات** (TASK-019)
  - `ChequeImport.php`
  - قالب: (customer_code, cheque_number, bank, due_date, amount, linked_issue_voucher_id nullable)
  - **عند الاعتماد**: إنشاء Cheque بحالة PENDING
  - Validation: customer_code موجود، due_date صالح
  - تحليل تاريخ: Carbon::parse()

#### Changed
- استبدال `maatwebsite/excel v1.1.5` بـ native PHP `fgetcsv()` للتوافق مع Laravel 12

---

### 📊 التقارير (TASK-020 إلى TASK-024)

#### Added
- **تقرير إجمالي المخزون** (TASK-020)
  - `ReportController::inventorySummary()`
  - استعلام: `product_branch_stock` مع Products
  - فلاتر: فرع، تصنيف، منتج، أقل من حد أدنى
  - جدول: SKU، الاسم، الفرع، الرصيد، الحد الأدنى، الحالة
  - ألوان: أخضر (normal)، أصفر (below min)، أحمر (out of stock)
  - **تصدير**: CSV (UTF-8) + PDF (A4 Landscape)
  - إحصائيات: إجمالي أصناف، كميات، تحت حد، نفذت
  - Pagination: 50 item/page

- **تقرير حركة صنف** (TASK-021)
  - `ReportController::productMovement()`
  - استعلام: `inventory_movements` WHERE product_id + فلتر تاريخ
  - جدول: التاريخ، النوع، الفرع، الكمية، المرجع، رصيد متحرك
  - Badges ملونة: ADD (green), ISSUE (red), RETURN (blue), TRANSFER_OUT (yellow), TRANSFER_IN (primary)
  - Validation: date_from ≤ date_to
  - تصدير: CSV + PDF

- **تقرير أرصدة العملاء** (TASK-022)
  - `ReportController::customerBalances()`
  - استعلام: مجموعة `customer_ledger_entries` لكل عميل
  - جدول: كود، اسم، رصيد، آخر نشاط، عدد فواتير، عدد مرتجعات
  - فلاتر: نوع رصيد (debit/credit/zero)، is_active
  - إحصائيات: إجمالي عملاء، إجمالي علية، إجمالي له، صافي الرصيد
  - Edge Case: عميل بدون حركات → رصيد=0

- **تقرير العملاء غير النشطين** (TASK-023)
  - `ReportController::inactiveCustomers()`
  - إعداد: `inactive_customer_months=12` (قابل للتعديل)
  - استعلام: `customers.last_activity_at < NOW() - INTERVAL N MONTH`
  - جدول: اسم، رصيد، آخر نشاط (diffForHumans)
  - Validation: N ≥ 1
  - Empty state: "جميع العملاء نشطون"

- **كشف حساب عميل PDF** (TASK-024)
  - `ReportController::customerStatement($customerId)`
  - Template: `customer-statement-pdf.blade.php`
  - **محتوى**:
    - رأس: بيانات عميل (code, name, phone, address)
    - صندوق ملخص (أزرق): إجمالي علية، له، الرصيد النهائي
    - جدول الحركات: تاريخ، بيان، علية، له، **رصيد متحرك**
    - توقيعات: محاسب، مدير مالي، عميل
  - فلتر: فترة (اختياري)
  - **حساب الرصيد المتحرك**:
    ```php
    $runningBalance = 0;
    foreach ($entries as $entry) {
        $runningBalance += ($entry->debit_aliah - $entry->credit_lah);
        $entry->running_balance = $runningBalance;
    }
    ```
  - زر "تصدير PDF" في صفحة دفتر العميل
  - Format: A4 Portrait، ألوان: أخضر (موجب)، أحمر (سالب)

---

### 🎯 لوحة المتابعة (TASK-025)

#### Added
- **DashboardController** مع 7 Widgets:
  1. **إحصائيات أساسية** (4 cards):
     - عدد الفروع النشطة
     - عدد التصنيفات
     - عدد المنتجات النشطة
     - إجمالي قيمة المخزون
  
  2. **أصناف أقل من الحد الأدنى**:
     - أعلى 10 مرتبة بالنقص الأكبر
     - لون: أصفر (warning)
  
  3. **أصناف نفذت**:
     - current_stock = 0
     - لون: أحمر (danger)
  
  4. **شيكات مستحقة قريباً**:
     - خلال 7 أيام
     - لون: أزرق (info)
  
  5. **شيكات متأخرة**:
     - due_date < NOW()
     - لون: أحمر (danger)
  
  6. **أكثر 10 أصناف حركة**:
     - الشهر الحالي
     - مجموع الحركات (SUM qty_units)
     - لون: أخضر (success)
  
  7. **آخر 10 أذون معتمدة**:
     - مرتبة بـ approved_at DESC
     - عرض: رقم، نوع (بيع/تحويل)، عميل/فرع، إجمالي

- **Dashboard View** (`dashboard.blade.php`):
  - تخطيط Bootstrap RTL
  - Icons ملونة (Bootstrap Icons)
  - روابط "عرض الكل" في تذييل كل card
  - Empty states مع رموز

---

### 🔧 تحسينات تقنية

#### Added
- **Routes**: 65+ route
- **Controllers**: 11 (Dashboard, Branch, Category, Product, Customer, IssueVoucher, ReturnVoucher, Payment, Cheque, Import, Report)
- **Models**: 14 مع Eloquent Relationships
- **Services**: 2 (SequencerService, InventoryService)
- **Import Classes**: 3 (CSV importers)
- **Views**: 45+ Blade templates
- **Migrations**: 16 migrations

#### Fixed
- **PowerShell Encoding**: استخدام `[System.Text.UTF8Encoding]::new($false)` لتجنب BOM
- **CSV UTF-8**: إضافة `Content-Type: text/csv; charset=UTF-8` في headers
- **Running Balance**: حساب تراكمي صحيح في كشف الحساب
- **Sequence Range**: تحقق صارم من نطاق 100,001-125,000 للارتجاع

#### Changed
- **Excel Import**: استبدال `maatwebsite/excel` بـ native `fgetcsv()` للتوافق
- **PDF Font**: DejaVu Sans كافتراضي لدعم العربية بشكل كامل

---

### 📚 الوثائق

#### Added
- **README.md**: دليل شامل (23KB) مع:
  - وصف كامل للمميزات
  - تعليمات التنصيب خطوة بخطوة
  - شرح الاستخدام لكل ميزة
  - مخطط البنية
  - استكشاف الأخطاء
  - إحصائيات المشروع

- **DEPLOYMENT-GUIDE.md**: دليل النشر على Hostinger (18KB):
  - خطوات التحضير قبل النشر
  - 3 طرق للرفع (File Manager, FTP, SSH)
  - إعداد قاعدة MySQL
  - ضبط `.htaccess` و `.env`
  - إعداد Cron Jobs
  - نسخ احتياطية تلقائية
  - استكشاف 10+ أخطاء شائعة
  - تحسين الأداء
  - Checklist قبل الإطلاق

- **CHANGELOG.md**: هذا الملف

- **ملفات موجودة مسبقاً**:
  - BACKLOG.md: 36 Task مفصلة
  - API-CONTRACT.md: توثيق API
  - PLAN.md: خطة المشروع
  - TEST-CASES.md: حالات الاختبار
  - QA-CHECKLIST.md: قائمة مراجعة الجودة
  - MIGRATIONS-ORDER.md: ترتيب Migrations

---

### 🎨 واجهة المستخدم

#### Added
- **RTL Support**: كامل مع Bootstrap 5.3 RTL
- **Arabic Font**: Cairo/Amiri في واجهة الويب، DejaVu Sans في PDF
- **Color Coding**:
  - أخضر: عادي، نجاح، علية (debit)
  - أحمر: خطر، نفذ، له (credit)
  - أصفر: تنبيه، تحت الحد
  - أزرق: معلومة، تحويل
- **Icons**: Bootstrap Icons في كل الواجهات
- **Badges**: ملونة لحالات مختلفة (DRAFT, APPROVED, PENDING, etc.)
- **Empty States**: رسائل ورموز عند عدم وجود بيانات

---

### 🔒 الأمان

#### Added
- **CSRF Protection**: على كل النماذج
- **SQL Injection Prevention**: Eloquent ORM + Prepared Statements
- **Validation**: صارمة على كل المدخلات مع رسائل عربية
- **Transaction Safety**: كل عمليات المخزون داخل DB Transactions
- **Negative Stock Prevention**: منع 100% للرصيد السالب

---

### ⚡ الأداء

#### Added
- **Eager Loading**: `with()` في كل الاستعلامات
- **Indexes**: على الأعمدة المستخدمة في WHERE/JOIN
- **Pagination**: 15-50 item/page حسب النوع
- **Cache**: Config/Route/View cache في الإنتاج
- **Optimized Queries**: استخدام `selectRaw()` و `groupBy()` بدل عدة استعلامات

---

### 📊 الإحصائيات النهائية

```
✅ Tasks مكتملة:      25/36 (69%)
📂 Files Created:      120+
📝 Lines of Code:      15,000+
🗄️ Database Tables:    16
🔀 Routes:             65+
🎨 Views:              45+
📊 Reports:            5
📥 Import Types:       3
🖨️ PDF Templates:      4
⚙️ Services:           2
📦 Models:             14
```

---

## [0.1.0] - 2025-09-30

### Added
- بداية المشروع
- إنشاء المستودع
- إعداد ملفات التخطيط (BACKLOG, PLAN, etc.)

---

## القادم

### نسخة 1.1.0 (متوقع: نوفمبر 2025)
- نظام الأدوار والصلاحيات الكامل
- سجل التدقيق (Activity Log)
- فلاتر متقدمة
- بحث فوري AJAX

### نسخة 1.2.0 (متوقع: ديسمبر 2025)
- واجهة موبايل Responsive
- تطبيق PWA
- Dark Mode

### نسخة 2.0.0 (متوقع: Q1 2026)
- API RESTful
- تطبيق React/Vue منفصل
- Multi-tenant Support

---

## الاصطلاحات

- `Added`: ميزة جديدة
- `Changed`: تغيير في ميزة موجودة
- `Deprecated`: ميزة ستُحذف قريباً
- `Removed`: ميزة مُحذفة
- `Fixed`: إصلاح bug
- `Security`: إصلاح أمني

---

**آخر تحديث**: 3 أكتوبر 2025

[Unreleased]: https://github.com/your-repo/inventory-system/compare/v1.0.0...HEAD
[1.0.0]: https://github.com/your-repo/inventory-system/releases/tag/v1.0.0
[0.1.0]: https://github.com/your-repo/inventory-system/releases/tag/v0.1.0
