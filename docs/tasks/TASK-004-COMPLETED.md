# ✅ TASK-004 & Products UI - Complete Documentation

**تاريخ الإنجاز:** 2 أكتوبر 2025  
**الحالة:** ✅ مكتمل 100%

---

## 📋 نظرة عامة

تم تنفيذ **TASK-004** (جدول المنتجات) بالكامل بالإضافة إلى واجهة مستخدم كاملة (ProductController + 4 Views) تتضمن:
- ✅ Migration للجدول products
- ✅ Product Model مع العلاقات الكاملة
- ✅ ProductSeeder بـ 8 منتجات نموذجية
- ✅ ProductController بجميع عمليات CRUD
- ✅ 4 صفحات: index, create, edit, show

---

## 🗄️ Database Migration

### الجدول: `products`

**الملف:** `database/migrations/2025_10_02_183044_create_products_table.php`

#### الحقول (Columns):
```
- id                (bigint, primary key)
- category_id       (FK → categories.id, onDelete: cascade)
- name              (varchar 200)
- description       (text, nullable)
- unit              (varchar 50) - وحدة القياس
- purchase_price    (decimal 10,2) - سعر الشراء
- sale_price        (decimal 10,2) - سعر البيع
- min_stock         (integer) - الحد الأدنى للمخزون
- is_active         (boolean, default true)
- created_at        (timestamp)
- updated_at        (timestamp)
```

#### الفهارس (Indexes):
```sql
$table->index('category_id');
$table->index('is_active');
$table->index('name'); -- للبحث السريع
```

---

## 🎨 Product Model

**الملف:** `app/Models/Product.php`

### Relationships (العلاقات)

#### 1. belongsTo - Category
```php
public function category()
{
    return $this->belongsTo(Category::class);
}
```
- **الغرض:** كل منتج ينتمي لتصنيف واحد
- **الاستخدام:** `$product->category->name`

#### 2. hasMany - ProductBranchStock
```php
public function branchStocks()
{
    return $this->hasMany(ProductBranchStock::class);
}
```
- **الغرض:** الحصول على جميع سجلات المخزون لهذا المنتج في كل الفروع
- **الاستخدام:** `$product->branchStocks` (مجموعة من ProductBranchStock)

#### 3. belongsToMany - Branch (Many-to-Many)
```php
public function branches()
{
    return $this->belongsToMany(Branch::class, 'product_branch_stock')
        ->withPivot('current_stock')
        ->withTimestamps();
}
```
- **الغرض:** الحصول على الفروع التي يتواجد بها المنتج مع بيانات المخزون
- **الاستخدام:** `$product->branches`

### Attributes (الخصائص المحسوبة)

#### getTotalStockAttribute
```php
public function getTotalStockAttribute()
{
    return $this->branchStocks()->sum('current_stock');
}
```
- **الغرض:** حساب إجمالي المخزون في كافة الفروع
- **الاستخدام:** `$product->total_stock` (بدون قوسين)

### Scopes (نطاقات الاستعلام)

#### 1. scopeActive
```php
public function scopeActive($query)
{
    return $query->where('is_active', true);
}
```
- **الاستخدام:** `Product::active()->get()`

#### 2. scopeSearch
```php
public function scopeSearch($query, $term)
{
    return $query->where('name', 'like', "%{$term}%")
                 ->orWhere('description', 'like', "%{$term}%");
}
```
- **الاستخدام:** `Product::search('لمبة')->get()`

### Fillable Fields
```php
protected $fillable = [
    'category_id',
    'name',
    'description',
    'unit',
    'purchase_price',
    'sale_price',
    'min_stock',
    'is_active',
];
```

### Casts
```php
protected $casts = [
    'purchase_price' => 'decimal:2',
    'sale_price' => 'decimal:2',
    'is_active' => 'boolean',
];
```

---

## 🌱 ProductSeeder

**الملف:** `database/seeders/ProductSeeder.php`

### البيانات المُدخلة (8 منتجات):

#### 1. لمبات LED
```
- لمبة LED 12 واط (التصنيف: لمبات LED, وحدة: قطعة)
  شراء: 25 ج.م | بيع: 35 ج.م | حد أدنى: 50 قطعة

- لمبة LED 20 واط (التصنيف: لمبات LED, وحدة: قطعة)
  شراء: 40 ج.م | بيع: 55 ج.م | حد أدنى: 30 قطعة
```

#### 2. مفاتيح كهربائية
```
- مفتاح فينوس أبيض (التصنيف: مفاتيح كهربائية, وحدة: قطعة)
  شراء: 15 ج.م | بيع: 22 ج.م | حد أدنى: 100 قطعة

- مفتاح مودرن فضي (التصنيف: مفاتيح كهربائية, وحدة: قطعة)
  شراء: 25 ج.م | بيع: 35 ج.م | حد أدنى: 50 قطعة
```

#### 3. أسلاك كهربائية
```
- سلك نحاس 1.5 مم (التصنيف: أسلاك كهربائية, وحدة: متر)
  شراء: 8 ج.م | بيع: 12 ج.م | حد أدنى: 200 متر

- سلك نحاس 2.5 مم (التصنيف: أسلاك كهربائية, وحدة: متر)
  شراء: 12 ج.م | بيع: 18 ج.م | حد أدنى: 150 متر
```

#### 4. قواطع كهربائية
```
- قاطع 32 أمبير (التصنيف: قواطع كهربائية, وحدة: قطعة)
  شراء: 80 ج.م | بيع: 110 ج.م | حد أدنى: 20 قطعة
```

#### 5. أباجورات
```
- أباجورة عمود معدني (التصنيف: أباجورات, وحدة: قطعة)
  شراء: 150 ج.م | بيع: 220 ج.م | حد أدنى: 10 قطعة
```

**الإجمالي:** 8 منتجات موزعة على 5 تصنيفات

---

## 🎮 ProductController

**الملف:** `app/Http/Controllers/ProductController.php`

### 1. index() - عرض قائمة المنتجات

**المسار:** `GET /products`  
**العرض:** `products.index`

#### الميزات:
- ✅ **البحث:** بالاسم أو الوصف
- ✅ **الفلترة:** حسب التصنيف أو الحالة (نشط/غير نشط)
- ✅ **الترقيم:** 15 منتج في الصفحة
- ✅ **Eager Loading:** لتقليل الاستعلامات (category, branchStocks.branch)

#### الكود:
```php
public function index(Request $request)
{
    $query = Product::with(['category', 'branchStocks.branch']);

    // البحث
    if ($request->filled('search')) {
        $query->search($request->search);
    }

    // فلترة حسب التصنيف
    if ($request->filled('category_id')) {
        $query->where('category_id', $request->category_id);
    }

    // فلترة حسب الحالة
    if ($request->filled('is_active')) {
        $query->where('is_active', $request->is_active);
    }

    $products = $query->orderBy('name')->paginate(15);
    $categories = Category::active()->orderBy('name')->get();

    return view('products.index', compact('products', 'categories'));
}
```

---

### 2. create() - نموذج إضافة منتج

**المسار:** `GET /products/create`  
**العرض:** `products.create`

#### البيانات المُرسلة:
```php
$categories = Category::active()->orderBy('name')->get();
$branches = Branch::active()->orderBy('name')->get();
```

---

### 3. store() - حفظ منتج جديد

**المسار:** `POST /products`  
**التحويل:** `redirect()->route('products.index')`

#### الميزات:
- ✅ **Validation كامل** مع رسائل عربية
- ✅ **DB Transaction** لضمان سلامة البيانات
- ✅ **توزيع المخزون الأولي** على الفروع (اختياري)

#### Validation Rules:
```php
[
    'category_id' => 'required|exists:categories,id',
    'name' => 'required|string|max:200',
    'description' => 'nullable|string',
    'unit' => 'required|string|max:50',
    'purchase_price' => 'required|numeric|min:0',
    'sale_price' => 'required|numeric|min:0',
    'min_stock' => 'required|integer|min:0',
    'is_active' => 'boolean',
]
```

#### المنطق الرئيسي:
```php
DB::transaction(function () use ($validated, $request) {
    // 1. إنشاء المنتج
    $product = Product::create($validated);

    // 2. إضافة المخزون الأولي لكل فرع (إن وُجد)
    if ($request->has('initial_stock')) {
        foreach ($request->initial_stock as $branchId => $stock) {
            if ($stock > 0) {
                ProductBranchStock::create([
                    'product_id' => $product->id,
                    'branch_id' => $branchId,
                    'current_stock' => $stock,
                ]);
            }
        }
    }
});
```

---

### 4. show() - عرض تفاصيل منتج

**المسار:** `GET /products/{id}`  
**العرض:** `products.show`

#### الميزات:
- ✅ عرض جميع بيانات المنتج
- ✅ حساب هامش الربح (Profit Margin %)
- ✅ عرض المخزون لكل فرع مع تلوين حسب الحالة
- ✅ إجمالي قيمة المخزون (بسعر الشراء وسعر البيع)
- ✅ الربح المتوقع

#### Eager Loading:
```php
$product = Product::with(['category', 'branchStocks.branch'])->findOrFail($id);
```

---

### 5. edit() - نموذج تعديل منتج

**المسار:** `GET /products/{id}/edit`  
**العرض:** `products.edit`

#### البيانات المُرسلة:
```php
$product = Product::with('branchStocks.branch')->findOrFail($id);
$categories = Category::active()->orderBy('name')->get();
```

**ملاحظة:** المخزون يُعرض للقراءة فقط (read-only) - لا يُعدّل من هنا

---

### 6. update() - تحديث منتج

**المسار:** `PUT /products/{id}`  
**التحويل:** `redirect()->route('products.index')`

#### Validation:
نفس قواعد store() بدون initial_stock

---

### 7. destroy() - حذف منتج

**المسار:** `DELETE /products/{id}`  
**التحويل:** `redirect()->route('products.index')`

#### ملاحظة:
- سيتم حذف جميع سجلات المخزون (product_branch_stock) تلقائياً بسبب `onDelete('cascade')`

---

## 🖼️ الصفحات (Views)

### 1️⃣ products/index.blade.php - قائمة المنتجات

**المسار:** `/products`

#### الأقسام:
1. **رأس الصفحة:**
   - العنوان: "إدارة المنتجات"
   - زر: "إضافة منتج جديد" (أخضر)

2. **نموذج البحث والفلترة:**
   - حقل البحث بالاسم
   - قائمة منسدلة: التصنيف (مع خيار "الكل")
   - قائمة منسدلة: الحالة (نشط/غير نشط/الكل)
   - زر: "بحث" (أزرق)
   - زر: "إعادة تعيين" (رمادي)

3. **جدول المنتجات:**
   | # | اسم المنتج | التصنيف | الوحدة | سعر الشراء | سعر البيع | المخزون | الحالة | الإجراءات |
   |---|------------|---------|--------|-----------|----------|---------|--------|-----------|

#### مميزات الجدول:
- ✅ عرض الاسم + الوصف المختصر (40 حرف)
- ✅ التصنيف كـ badge أزرق
- ✅ الأسعار مُنسّقة (`number_format`)
- ✅ المخزون:
  - **إجمالي** (مجموع كل الفروع)
  - **Badge أخضر** إذا أعلى من min_stock
  - **Badge أصفر + أيقونة تحذير** إذا أقل من min_stock
  - عرض تفصيلي لكل فرع أسفل الإجمالي: `FAC: 45 | ATB: 30 | IMB: 50`
- ✅ الحالة: badge أخضر (نشط) / أحمر (غير نشط)
- ✅ الإجراءات (3 أزرار):
  - 👁️ **عرض** (أزرق) → `/products/{id}`
  - ✏️ **تعديل** (أصفر) → `/products/{id}/edit`
  - 🗑️ **حذف** (أحمر) → مع تأكيد JavaScript

4. **Pagination:**
   - روابط الصفحات في الأسفل (Bootstrap 5)

---

### 2️⃣ products/create.blade.php - إضافة منتج جديد

**المسار:** `/products/create`

#### الأقسام:

**أ) بيانات المنتج (Column 1):**
- التصنيف (قائمة منسدلة، مطلوب)
- اسم المنتج (نص، مطلوب، max:200)
- الوصف (textarea، اختياري)
- وحدة القياس (نص، مطلوب، مثال: قطعة/متر/كرتونة)

**ب) الأسعار والمخزون (Column 2):**
- سعر الشراء (رقم، مطلوب، min:0)
- سعر البيع (رقم، مطلوب، min:0)
- الحد الأدنى للمخزون (عدد صحيح، مطلوب)
- منتج نشط (checkbox، افتراضياً مُفعّل)

**ج) المخزون الأولي لكل فرع (اختياري):**
- 3 حقول (واحد لكل فرع):
  - 🏭 المصنع
  - 🏢 العتبة
  - 🏪 إمبابة
- قيمة افتراضية: 0

**د) الأزرار:**
- 💾 **حفظ المنتج** (أزرق)
- ❌ **إلغاء** (رمادي، رجوع لـ `/products`)

#### الميزات:
- ✅ عرض الأخطاء أسفل كل حقل (`@error`)
- ✅ استرجاع القيم القديمة (`old()`)
- ✅ تصميم responsive (row → col-md-6)

---

### 3️⃣ products/edit.blade.php - تعديل منتج

**المسار:** `/products/{id}/edit`

#### الفرق عن create:
- ✅ عنوان: "تعديل المنتج: {name}"
- ✅ القيم مملوءة مسبقاً (`old('name', $product->name)`)
- ✅ عرض المخزون الحالي لكل فرع (read-only):
  ```
  المخزون الحالي:
  - المصنع: 45 قطعة
  - العتبة: 30 قطعة
  - إمبابة: 50 قطعة
  ```
- ✅ زر: "حفظ التعديلات"
- ✅ Method: `@method('PUT')`

**ملاحظة:** المخزون لا يُعدّل من هنا - سيُعدّل لاحقاً عبر أذون الصرف/الإرجاع

---

### 4️⃣ products/show.blade.php - تفاصيل المنتج

**المسار:** `/products/{id}`

#### الأقسام (4 بطاقات):

**أ) بطاقة معلومات المنتج (Card 1 - أزرق):**
```
التصنيف: [badge أزرق]
اسم المنتج: ...
الوصف: ...
وحدة القياس: قطعة
الحالة: [badge أخضر: نشط / أحمر: غير نشط]
تاريخ الإنشاء: 2025-10-02 20:15
```

**ب) بطاقة الأسعار والربح (Card 2 - أخضر):**
```
سعر الشراء: 25.00 ج.م
سعر البيع: 35.00 ج.م
هامش الربح: 40.00%  [حساب: (البيع - الشراء) / البيع × 100]
الحد الأدنى للمخزون: 50 قطعة
```

**ج) المخزون في كل فرع (3 Cards - أزرق/أصفر/أحمر):**

كل فرع له بطاقة منفصلة:
```
🏭 المخزون في: المصنع (FAC)
├─ المخزون الحالي: 45 قطعة
└─ الحالة: [badge أخضر: "متوفر" إذا > min_stock]
           [badge أصفر: "منخفض" إذا < min_stock]
           [badge أحمر: "نفذ" إذا = 0]
```

- **لون البطاقة:**
  - حدود خضراء: stock > min_stock
  - حدود صفراء: 0 < stock < min_stock
  - حدود حمراء: stock = 0

**د) الملخص المالي (Card 4 - ذهبي):**
```
إجمالي المخزون: 125 قطعة  [مجموع الفروع الثلاثة]

إجمالي القيمة (بسعر الشراء): 3,125.00 ج.م  [125 × 25]
إجمالي القيمة (بسعر البيع): 4,375.00 ج.م   [125 × 35]
الربح المتوقع: 1,250.00 ج.م  [الفرق]

عدد الفروع: 3
```

**تنبيهات:**
- إذا كان إجمالي المخزون < min_stock:
  ```
  ⚠️ تنبيه: المخزون الإجمالي أقل من الحد الأدنى!
  ```

#### الأزرار (أعلى الصفحة):
- ✏️ **تعديل** (أصفر)
- ⬅️ **رجوع** (رمادي)

---

## 📊 الإحصائيات

### قاعدة البيانات:
```
✅ 3 فروع
✅ 6 تصنيفات
✅ 8 منتجات
✅ 24 سجل مخزون (8 × 3)
```

### الكود:
```
✅ 1 Migration
✅ 1 Model مع 3 relationships + 1 attribute + 2 scopes
✅ 1 Seeder
✅ 1 Controller مع 7 methods
✅ 4 Views (index, create, edit, show)
✅ 1 Route resource (يولد 7 routes)
```

---

## 🧪 الاختبار

تم الاختبار اليدوي:
- ✅ `/products` - عرض القائمة + البحث + الفلترة + Pagination
- ✅ `/products/create` - إضافة منتج مع مخزون أولي
- ✅ `/products/{id}` - عرض التفاصيل + الحسابات الصحيحة
- ✅ `/products/{id}/edit` - تعديل البيانات

**تم التأكيد من:**
- الأسعار تظهر بصيغة صحيحة (decimal 2)
- المخزون يُحسب صح (sum من كل الفروع)
- التحذيرات تظهر للمخزون المنخفض
- Eager Loading يعمل (استعلامات قليلة)

---

## 🎯 المخرجات النهائية

| الملف | الحالة | الوصف |
|------|-------|-------|
| `create_products_table.php` | ✅ | Migration منفذة |
| `Product.php` | ✅ | Model كامل |
| `ProductSeeder.php` | ✅ | 8 منتجات مُدخلة |
| `ProductController.php` | ✅ | CRUD كامل |
| `products/index.blade.php` | ✅ | قائمة + بحث + فلترة |
| `products/create.blade.php` | ✅ | إضافة + مخزون أولي |
| `products/edit.blade.php` | ✅ | تعديل |
| `products/show.blade.php` | ✅ | تفاصيل + تحليلات |
| `web.php` | ✅ | Route::resource |

---

## ✨ الميزات البارزة

1. **البحث والفلترة المتقدمة** في صفحة index
2. **المخزون الأولي** عند إضافة منتج جديد (توزيع على الفروع)
3. **DB Transaction** في store() لضمان سلامة البيانات
4. **Eager Loading** لتقليل الاستعلامات
5. **صفحة تفاصيل غنية** (show) مع:
   - حساب هامش الربح
   - تلوين حسب حالة المخزون
   - إجمالي القيمة المالية
   - الربح المتوقع
6. **Validation كامل** مع رسائل عربية
7. **تصميم RTL** متجاوب مع Bootstrap 5.3

---

## 🔗 الروابط ذات الصلة

- **TASK-006:** جدول product_branch_stock (Many-to-Many Pivot)
- **TASK-003:** جدول categories
- **BACKLOG.md:** المهام القادمة (Vouchers, Transfers, Reports...)

---

**النتيجة:** نظام إدارة منتجات متكامل جاهز للاستخدام! 🎉
