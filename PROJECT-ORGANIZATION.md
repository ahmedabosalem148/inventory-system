# 📁 Project Organization Guide

تم إعادة تنظيم المشروع بشكل كامل لتسهيل التنقل والصيانة.

## 📂 هيكل المجلدات الجديد

### 📘 `/docs` - الوثائق
```
docs/
├── tasks/              # جميع التاسكات المكتملة (TASK-*.md)
├── sessions/           # سجلات الجلسات (SESSION-*.md)
├── reports/            # التقارير الفنية (*REPORT*.md)
├── fixes/              # وثائق الإصلاحات (*-FIX*, *-COMPLETED.md)
├── architecture/       # التصميم المعماري (*ARCHITECTURE*, *LOGIC*, *FLOW*)
├── *.md               # وثائق المشروع الأساسية
```

### 🛠️ `/scripts` - السكريبتات
```
scripts/
├── testing/           # سكريبتات الاختبار (test_*.php)
├── utilities/         # السكريبتات المساعدة
│   ├── check_*.php   # فحص البيانات
│   ├── create_*.php  # إنشاء بيانات تجريبية
│   ├── assign_*.php  # تعيين الصلاحيات
│   ├── get_*.php     # جلب المعلومات
│   ├── fix_*.php     # إصلاحات سريعة
│   └── seed_*.php    # ملء البيانات
```

### 💻 `/app` - كود التطبيق الأساسي
```
app/
├── Http/
│   ├── Controllers/  # Controllers
│   └── Middleware/   # Middleware
├── Models/           # Models
├── Services/         # Business Logic
└── Policies/         # Authorization
```

### 🌐 Frontend
```
frontend/             # Vue.js Frontend
client-react/         # React Client (إن وجد)
```

### ⚙️ Configuration
```
config/              # Laravel Configuration
database/            # Migrations & Seeders
routes/              # API Routes
tests/               # PHPUnit Tests
```

## 🎯 الملفات الرئيسية

- `README.md` - الوثائق الرئيسية للمشروع
- `composer.json` - PHP Dependencies
- `package.json` - JavaScript Dependencies
- `docker-compose.yml` - Docker Configuration
- `Makefile` - أوامر سريعة للمشروع

## 📋 كيفية الاستخدام

### تشغيل السكريبتات المساعدة
```bash
# من مجلد scripts/utilities
php scripts/utilities/check_users.php
php scripts/utilities/create_admin.php
```

### تشغيل الاختبارات
```bash
# من مجلد scripts/testing
php scripts/testing/test_api_request.php
php scripts/testing/test_branch_transfers.php
```

### الوصول للوثائق
- **التاسكات المكتملة**: `docs/tasks/`
- **سجلات الجلسات**: `docs/sessions/`
- **التقارير الفنية**: `docs/reports/`
- **وثائق الإصلاحات**: `docs/fixes/`
- **التصميم المعماري**: `docs/architecture/`

## 🔍 البحث السريع

### للبحث عن تاسك معين:
```bash
# ابحث في مجلد docs/tasks
Get-ChildItem docs/tasks/ -Filter "*TASK-*.md"
```

### للبحث عن جلسة معينة:
```bash
# ابحث في مجلد docs/sessions
Get-ChildItem docs/sessions/ -Filter "*SESSION-*.md"
```

## ✅ فوائد التنظيم الجديد

1. **سهولة الوصول** - كل نوع من الملفات في مجلده المخصص
2. **الوضوح** - الجذر نظيف ويحتوي فقط على الملفات الأساسية
3. **الصيانة** - سهولة إيجاد وتحديث الملفات
4. **التوثيق** - كل الوثائق منظمة حسب النوع
5. **السكريبتات** - السكريبتات مصنفة حسب الوظيفة

## 📌 ملاحظات

- جميع ملفات الوثائق السابقة تم الاحتفاظ بها
- لم يتم حذف أي ملفات، فقط إعادة تنظيم
- المسارات القديمة لن تعمل، استخدم المسارات الجديدة
- تم نقل الملفات المساعدة (HTML, JSON) إلى `scripts/utilities/`

---
**آخر تحديث**: 17 أكتوبر 2025
