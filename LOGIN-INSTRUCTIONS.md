# 🔐 تعليمات تسجيل الدخول

## المشكلة الحالية

الـ API بيرجع **403 Forbidden** لأن المستخدم مش مسجل دخول!

```
127.0.0.1:8000/api/v1/dashboard:1  Failed to load resource: 403 (Forbidden)
127.0.0.1:8000/api/v1/return-vouchers:1  Failed to load resource: 403 (Forbidden)
```

---

## ✅ الحل

### الخطوة 1: افتح صفحة تسجيل الدخول

افتح المتصفح واذهب إلى:
```
http://localhost:3000/login
```

أو اضغط على أي رابط في النظام، الـ ProtectedRoute هيحولك تلقائياً لصفحة Login.

---

### الخطوة 2: استخدم البيانات التجريبية

الصفحة فيها بيانات تجريبية جاهزة:

📧 **Email:** `test@example.com`  
🔑 **Password:** `password`

أو اضغط زر **"انقر لملء البيانات تلقائياً"** وهيملأ الحقول لوحده!

---

### الخطوة 3: اضغط "تسجيل الدخول"

بعد كده النظام هيحفظ الـ token في localStorage وهيشتغل كل حاجة! ✅

---

## 🧪 تأكيد نجاح Login

بعد تسجيل الدخول، افتح Developer Console وشوف:

```javascript
// في Console اكتب:
localStorage.getItem('token')

// لو رجع string طويل يبقى تمام ✅
// لو رجع null يبقى فيه مشكلة ❌
```

---

## 🎯 بيانات مستخدمين أخرى (من Database)

### Super Admin
- **Email:** `manager@inventory.local`
- **Password:** `password`
- **Role:** مدير النظام (كل الصلاحيات)

### Store User
- **Email:** `store1@inventory.local`
- **Password:** `password`
- **Role:** مستخدم فرع (صلاحيات محدودة)

### Accounting
- **Email:** `accounting@inventory.local`
- **Password:** `password`
- **Role:** محاسب (مراجعة فقط)

---

## 🔧 إذا استمرت المشكلة

### 1. تأكد من Laravel Server شغال
```bash
php artisan serve
```

يجب أن يكون على: `http://127.0.0.1:8000`

### 2. تأكد من Frontend Server شغال
```bash
cd frontend
npm run dev
```

يجب أن يكون على: `http://localhost:3000`

### 3. تأكد من .env في Frontend
```
VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1
```

### 4. Clear Cache
```bash
# في المتصفح:
- اضغط F12
- Application tab
- Clear Storage
- Clear site data
- Refresh
```

---

## ✅ بعد Login الناجح

هتقدر تستخدم:
- ✅ Dashboard (بيانات حقيقية)
- ✅ Products (CRUD كامل)
- ✅ Customers (إضافة/تعديل/حذف)
- ✅ Issue Vouchers (إنشاء أذونات صرف)
- ✅ Return Vouchers (إنشاء أذونات إرجاع)

---

## 🎉 تهانينا!

بعد Login، النظام شغال 100%! 🚀

